If you don’t have it, please install node.js, npm (the installer that comes with node.js), Naked (via pip install naked), and google-play-scraper 
(npm google-play-scraper) before running! To run, just execute main.py.
